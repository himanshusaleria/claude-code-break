{
  "intensity_weights": {
    "micro": 0.6,
    "light": 0.3,
    "active": 0.1
  },
  "cooldown_seconds": 90,
  "daily_cap_active": 40,
  "channels": {
    "notification": true,
    "terminal": true,
    "tts": false
  },
  "notification_title": "Movement Break",
  "trigger_events": ["Notification", "Stop"],
  "_comment": "trigger_events: which Claude Code hook events fire a prompt. 'Notification' = idle/waiting. 'Stop' = assistant finished. 'PreToolUse' = every tool call (uses cooldown). Use 'Notification' only for the calmest default."
}
